[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "neuronauts"
version = "0.1.0"
description = "Reconstruct neurons from EM connectome (CAVE) synapses: lineage-supervised representation learning + synapse-graph cell assignment, scored as line-graph F1."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.0",
    "urllib3<3",
    "charset-normalizer<4",
    "chardet<6",
    "numpy",
    "scipy",
    "truststore",
    "pyarrow",  # Arrow IPC payloads from the CAVE materialization API (data/lineage.py)
]

[project.optional-dependencies]
dev = [
    "matplotlib",
    "pytest",
    "pandas",
    "scikit-learn",
]
cave = [
    "caveclient",
    "pandas",
]
topology = [
    "torch",
]
membrane = [
    "torch",
    "tifffile",
]
claude = [
    "anthropic>=0.25",
]
gemini = [
    "google-generativeai>=0.5",
]
llm = [
    "anthropic>=0.25",
    "google-generativeai>=0.5",
]

[project.scripts]
neuronauts = "neuronauts.legacy.run:main"

[tool.setuptools]
packages = [
    "neuronauts",
    "neuronauts.legacy",
    "neuronauts.coassign",
    "neuronauts.data",
    "neuronauts.represent",
    "neuronauts.assemble",
    "neuronauts.meshing",
    "neuronauts.harness",
    "neuronauts.report",
    "treestitch",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "legacy: v1 agent/membrane simulation tests that exercise neuronauts.legacy; run with -m 'not legacy' to skip",
]
